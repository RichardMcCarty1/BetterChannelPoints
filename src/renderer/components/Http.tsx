import { ReactElement } from 'react';
import Box from '@mui/material/Box';

export const Http = (): ReactElement => {

  return (
    <Box sx={{
      display: 'flex',
      width: '100%',
      height: '100%',
      alignItems: 'flex-start',
      justifyContent: 'center',
      flexDirection: 'row'
    }}>
      <div style={{ paddingBottom: '10px' }}>
        cum
      </div>
    </Box>
  );
};
